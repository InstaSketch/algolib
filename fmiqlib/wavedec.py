import config
import numpy as np
import pywt
import copy
import Image

def wave_transform(img, method):
	""" Transforms a 2D image in the form of a numpy array into
			its level 1 wavelet decomposition.
			"""
	return img

def process_img(img, m):
	"""Sets all but the m largest coefficients to 0"""
	return img

def quantize_img(img):
	"""Quantizes image into +1, 0, or -1"""
	return img