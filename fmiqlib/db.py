import config
import numpy as np
import shelve
import sys

def size():
	"""Returns total number of images"""
	return -1

def add_img(id, path):
	"""Image ID to path mapping"""

def add_img_fp(id, img):
	"""img is supposed to be the output from either process_img or quantize_img; adds the coefficients into their respective search"""

def genid(img):
	"""Generates a unique ID from an image"""